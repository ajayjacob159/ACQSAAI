name: ACQSA AI 24/7 Cloud Client Acquisition & LinkedIn Swarm

on:
  schedule:
    # Runs every 15 minutes 24/7/365 in GitHub Cloud even when laptop is shutdown
    - cron: '*/15 * * * *'
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  run-cloud-swarm:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.10 Runtime
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install websockets requests || echo "Core standard library ready"

      - name: Run 24/7 Healthcare Client Acquisition Swarm Cycle
        env:
          TELEGRAM_BOT_TOKEN: "8605382934:AAEYaehMipYzjn4VWKVDmSVLyNonf9_-QYo"
          TELEGRAM_CHAT_ID: "8976047512"
          SMTP_HOST: "smtp.hostinger.com"
          SMTP_PORT: "465"
          SMTP_USER: "ceo@acqsaai.com"
          SMTP_PASS: "9030531872@Ajay"
        run: |
          python3 client_acquisition_swarm/swarm_master_runner.py --single

      - name: Run LinkedIn Adrenal & Engagement Cloud Swarm
        env:
          TELEGRAM_BOT_TOKEN: "8605382934:AAEYaehMipYzjn4VWKVDmSVLyNonf9_-QYo"
          TELEGRAM_CHAT_ID: "8976047512"
        run: |
          python3 client_acquisition_swarm/acqsa_linkedin_autonomous_adrenal_swarm.py || echo "LinkedIn Cloud Cycle Complete"
